owlapy.agen_kg.domain_examples_cache
====================================

.. py:module:: owlapy.agen_kg.domain_examples_cache

.. autoapi-nested-parse::

   Domain-Specific Few-Shot Examples Cache Manager

   This module manages caching of domain-specific few-shot examples generated by the
   DomainGraphExtractor. Examples are saved to and loaded from JSON files organized by domain.

   The cache helps avoid regenerating examples for the same domain multiple times,
   improving performance when processing multiple documents from the same domain.



Attributes
----------

.. autoapisummary::

   owlapy.agen_kg.domain_examples_cache.EXAMPLE_TYPE_MAPPING
   owlapy.agen_kg.domain_examples_cache.VARIABLE_NAME_TO_TYPE


Classes
-------

.. autoapisummary::

   owlapy.agen_kg.domain_examples_cache.DomainExamplesCache


Module Contents
---------------

.. py:data:: EXAMPLE_TYPE_MAPPING

.. py:data:: VARIABLE_NAME_TO_TYPE

.. py:class:: DomainExamplesCache(cache_dir: Optional[str] = None)

   Manages caching of domain-specific few-shot examples.

   Examples are stored in JSON format with the filename pattern:
   domain_examples_{domain_name}.json

   The cache directory defaults to the current working directory but can be configured.


   .. py:attribute:: cache_dir


   .. py:method:: save_examples(domain: str, examples: Dict[str, str]) -> bool

      Save generated examples for a domain.

      :param domain: The domain name.
      :param examples: Dictionary mapping example types to example strings.
                       Expected keys are from EXAMPLE_TYPE_MAPPING.keys()

      :returns: True if save was successful, False otherwise.



   .. py:method:: load_examples(domain: str) -> Optional[Dict[str, str]]

      Load cached examples for a domain.

      :param domain: The domain name.

      :returns: Dictionary mapping example types to example strings, or None if not cached.



   .. py:method:: examples_exist(domain: str) -> bool

      Check if cached examples exist for a domain.

      :param domain: The domain name.

      :returns: True if examples are cached, False otherwise.



   .. py:method:: get_cache_file_path(domain: str) -> str

      Get the full path to the cache file for a domain.

      :param domain: The domain name.

      :returns: String path to the cache file.



   .. py:method:: clear_domain_cache(domain: str) -> bool

      Delete the cache file for a domain.

      :param domain: The domain name.

      :returns: True if deletion was successful, False otherwise.



   .. py:method:: clear_all_caches() -> bool

      Delete all domain example cache files.

      :returns: True if all deletions were successful, False otherwise.



   .. py:method:: list_cached_domains() -> list

      List all domains that have cached examples.

      :returns: List of domain names with cached examples.



